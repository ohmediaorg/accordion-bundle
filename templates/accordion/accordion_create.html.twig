{% extends '@OHMediaBackend/form.html.twig' %}

{% set form_title = 'Create Accordion' %}

{% block breadcrumbs %}
  {{ bootstrap_breadcrumbs(
    bootstrap_breadcrumb(bootstrap_icon('menu-button-wide') ~ ' Accordions', 'accordion_index'),
    bootstrap_breadcrumb('Create'),
  ) }}
{% endblock %}
