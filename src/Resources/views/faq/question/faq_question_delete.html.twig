{% extends '@OHMediaBackend/form.html.twig' %}

{% set form_title = 'Delete ' ~ accordion_item %}

{% block breadcrumbs %}
{{ bootstrap_breadcrumbs(
  bootstrap_breadcrumb(bootstrap_icon('question-circle-fill') ~ ' FAQs', 'faq_index'),
  bootstrap_breadcrumb(accordion, 'faq_view', {id: accordion.id}),
  bootstrap_breadcrumb('Delete Question'),
) }}
{% endblock %}
