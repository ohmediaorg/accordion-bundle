{% extends '@OHMediaBackend/form.html.twig' %}

{% set form_title = 'Delete ' ~ accordion_item %}

{% block breadcrumbs %}
{{ bootstrap_breadcrumbs(
  bootstrap_breadcrumb(bootstrap_icon('arrow-down-square-fill') ~ ' Accordions', 'faq_index'),
  bootstrap_breadcrumb(accordion, 'accordion_view', {id: accordion.id}),
  bootstrap_breadcrumb('Delete Item'),
) }}
{% endblock %}
